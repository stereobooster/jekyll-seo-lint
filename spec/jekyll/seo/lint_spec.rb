require 'spec_helper'

describe Jekyll::Seo::Lint do
  it 'has a version number' do
    expect(Jekyll::Seo::Lint::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
